<div class="page" id="upside">
  <%= paginate @games %>
  <%= page_entries_info @games %>
</div>

<ul class="users">
  <%= render @games %>
</ul>

<div class="page">
  <%= paginate @games %>
  <%= page_entries_info @games %>
</div>
