<% if user.profile_image.attached? %>
  <%= image_tag user.profile_image.variant(resize:yield(:size)).processed, class: "profile_image" %>
<% else %>
  <%= image_tag 'NO_IMAGE2.jpeg', class: "no_image" %>
<% end %>
