<h1>すべてのユーザ</h1>

<div class="page">
  <%= paginate @users %>
  <%= page_entries_info @users %>
</div>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 80 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<div class="page">
  <%= paginate @users %>
  <%= page_entries_info @users %>
</div>
